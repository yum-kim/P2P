export enum BoardStatus {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}

export enum BoardSortTypeEnum {
  createAt = 'createAt',
}

export enum BoardHeartTypeEnum {
  increment = 'increment',
  decrement = 'decrement',
}
